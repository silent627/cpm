# JMeter获取用户信息接口测试方案

## 1. 测试计划结构

```
获取用户信息测试计划
├── 测试计划
│   ├── HTTP信息头管理器
│   ├── CSV数据集配置（Token）
│   ├── 线程组
│   │   ├── HTTP请求（获取用户信息）
│   │   ├── JSON断言
│   │   └── 响应断言
│   └── 监听器（聚合报告）
```

## 2. 配置步骤

### 2.1 测试计划配置

- **名称**：获取用户信息接口性能测试
- **注释**：测试获取当前用户信息接口的并发性能

### 2.2 HTTP信息头管理器

**位置**：测试计划 > 添加 > 配置元件 > HTTP信息头管理器

| 名称 | 值 |
|------|-----|
| Authorization | `Bearer ${token}` |
| Content-Type | application/json |

**说明**：`${token}` 变量从CSV文件或前置登录请求中获取

### 2.3 Token数据准备

#### 方案A：使用CSV文件（推荐，适合已获取Token的场景）

**位置**：测试计划 > 添加 > 配置元件 > CSV数据集配置

| 参数 | 值 | 说明 |
|------|-----|------|
| 文件名 | `tokens.csv` | Token文件路径 |
| 变量名称 | `token` | Token变量名 |
| 忽略首行 | `false` | 如果CSV有表头则设为true |
| 分隔符 | `,` | CSV分隔符 |
| 是否允许带引号 | `true` | 允许CSV字段带引号 |
| 遇到文件结束符再次循环 | `true` | 循环使用数据 |
| 遇到文件结束符停止线程 | `false` | 不停止线程 |

**tokens.csv 文件内容示例：**
```csv
token
eyJhbGciOiJIUzUxMiJ9.eyJ1c2VySWQiOjEsInVzZXJuYW1lIjoiYWRtaW4iLCJyb2xlIjoiQURNSU4iLCJleHAiOjE3MDAwMDAwMDB9.xxx
eyJhbGciOiJIUzUxMiJ9.eyJ1c2VySWQiOjIsInVzZXJuYW1lIjoidXNlcjEiLCJyb2xlIjoiVVNFUiIsImV4cCI6MTcwMDAwMDAwMH0.yyy
```

#### 方案B：先登录再获取信息（组合测试）

**位置**：线程组 > 添加 > 取样器 > HTTP请求（登录）

配置登录请求，提取Token后用于后续请求（参考登录接口测试方案）

### 2.4 线程组配置

**位置**：测试计划 > 添加 > 线程（用户）> 线程组

| 参数 | 值 | 说明 |
|------|-----|------|
| 线程数 | `100` | 并发用户数 |
| Ramp-up时间 | `10` | 10秒内启动100个线程 |
| 循环次数 | `10` | 每个线程执行10次 |
| 调度器 | 勾选 | 启用调度器 |
| 持续时间 | `300` | 运行300秒（5分钟） |

### 2.5 HTTP请求配置

**位置**：线程组 > 添加 > 取样器 > HTTP请求

| 参数 | 值 |
|------|-----|
| 名称 | 获取当前用户信息 |
| 协议 | `http` |
| 服务器名称或IP | `localhost` |
| 端口号 | `8080` |
| 方法 | `GET` |
| 路径 | `/api/user/info` |
| 内容编码 | `UTF-8` |

**请求头**（已在HTTP信息头管理器中配置）：
- Authorization: `Bearer ${token}`

### 2.6 JSON断言

**位置**：HTTP请求 > 添加 > 断言 > JSON断言

| 参数 | 值 |
|------|-----|
| Assert JSON Path exists | `$.code` |
| Additionally assert value | 勾选 |
| Expected Value | `200` |
| Match | `Equals` |

### 2.7 响应断言（验证返回数据）

**位置**：HTTP请求 > 添加 > 断言 > 响应断言

| 参数 | 值 |
|------|-----|
| 响应字段 | 响应文本 |
| 模式匹配规则 | 包含 |
| 测试模式 | `"username"` |

**说明**：验证响应中包含username字段，确保返回了用户信息

### 2.8 监听器配置

#### 2.8.1 聚合报告（必需）

**位置**：线程组 > 添加 > 监听器 > 聚合报告

**作用**：统计响应时间、吞吐量、错误率等关键指标

#### 2.8.2 查看结果树（调试用，正式测试可禁用）

**位置**：线程组 > 添加 > 监听器 > 查看结果树

**说明**：仅用于调试，正式测试时建议禁用

## 3. 性能指标配置

### 3.1 响应时间要求

- P50（中位数）：≤ 200ms
- P95：≤ 500ms
- P99：≤ 1000ms

### 3.2 吞吐量要求

- QPS：≥ 500

### 3.3 成功率要求

- 成功率：≥ 99.9%

## 4. 测试场景

### 场景1：正常负载测试
- 线程数：100
- Ramp-up：10秒
- 持续时间：5分钟

### 场景2：峰值负载测试
- 线程数：300
- Ramp-up：30秒
- 持续时间：10分钟

### 场景3：压力测试
- 线程数：500
- Ramp-up：60秒
- 持续时间：15分钟

## 5. Token获取方法

### 方法1：批量获取Token脚本

使用登录接口批量获取Token并保存到CSV文件：

```bash
# 使用curl批量获取Token（示例）
for user in $(cat users.csv); do
  token=$(curl -s -X POST http://localhost:8080/api/auth/login \
    -H "Content-Type: application/json" \
    -d "{\"username\":\"$user\",\"password\":\"123456\"}" \
    | jq -r '.data.token')
  echo "$token" >> tokens.csv
done
```

### 方法2：JMeter组合测试

在测试计划中添加登录请求作为前置步骤：

```
线程组
├── HTTP请求（登录）
│   ├── JSON断言
│   └── 正则表达式提取器（提取Token）
└── HTTP请求（获取用户信息）
    └── JSON断言
```

**配置要点**：
- 登录请求提取Token到变量 `${token}`
- 获取用户信息请求使用 `${token}` 变量

## 6. 执行步骤

1. **准备Token数据**：
   - 方案A：使用批量脚本获取Token并保存到`tokens.csv`
   - 方案B：使用组合测试（登录+获取信息）

2. **配置测试计划**：按照上述步骤配置JMeter测试计划

3. **保存测试计划**：保存为`获取用户信息接口测试.jmx`

4. **执行测试**：
   ```bash
   # 命令行执行（无GUI模式，性能更好）
   jmeter -n -t 获取用户信息接口测试.jmx -l 测试结果.jtl -e -o 测试报告/
   ```

5. **查看报告**：打开生成的HTML报告查看详细结果

## 7. 结果分析要点

- **响应时间**：关注P95和P99响应时间
- **吞吐量**：查看QPS是否达到要求
- **错误率**：检查401未授权错误（Token过期或无效）
- **资源使用**：结合系统监控查看CPU、内存使用情况

## 8. 注意事项

1. **Token有效性**：
   - Token有效期为24小时，确保测试期间Token未过期
   - 如Token过期，需要重新获取

2. **Token数量**：
   - 准备足够的Token（至少与并发用户数相同）
   - 建议准备Token数量 = 并发用户数 × 1.5（冗余）

3. **认证失败处理**：
   - 监控401错误，可能是Token过期或无效
   - 设置合理的断言，区分认证失败和其他错误

4. **限流处理**：
   - 注意系统限流配置（每分钟180次请求）
   - 如需取消限流，参考限流配置文档

5. **测试环境**：
   - 确保测试环境与生产环境配置一致
   - 监控应用服务器、数据库、Redis等资源

## 9. 快速配置模板

**最小化配置（仅必需组件）：**
1. 线程组
2. HTTP信息头管理器（设置Authorization）
3. CSV数据集配置（Token数据）
4. HTTP请求（GET /api/user/info）
5. JSON断言
6. 聚合报告

**禁用组件（节省资源）：**
- 查看结果树（正式测试时）
- 图形结果
- 其他非必需监听器

## 10. 常见问题

### Q1: Token过期怎么办？
**A**: 定期刷新Token，或使用组合测试方案（每次先登录再获取信息）

### Q2: 401未授权错误过多？
**A**: 检查Token是否有效，确保CSV文件中的Token格式正确

### Q3: 如何验证不同用户的Token？
**A**: 使用CSV文件，每行一个Token，JMeter会循环使用

### Q4: 能否使用同一个Token？
**A**: 可以，但建议使用多个Token模拟真实场景，避免单点问题
